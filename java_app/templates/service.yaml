apiVersion: v1
kind: Service
metadata:
  name: {{ include "java_app.fullname" . }}
  labels:
    {{- include "java_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java_app.selectorLabels" . | nindent 4 }}
